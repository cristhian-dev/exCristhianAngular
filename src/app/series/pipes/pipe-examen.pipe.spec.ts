import { PipeExamenPipe } from './pipe-examen.pipe';

describe('PipeExamenPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeExamenPipe();
    expect(pipe).toBeTruthy();
  });
});
