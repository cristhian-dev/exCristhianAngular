import { RoleformatPipe } from './roleformat.pipe';

describe('RoleformatPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleformatPipe();
    expect(pipe).toBeTruthy();
  });
});
